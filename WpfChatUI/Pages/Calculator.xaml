<Page x:Class="WpfChatUI.Pages.Calculator"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfChatUI.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="800"
      Title="Calculator">
    <Page.Resources>
        <Style x:Key="TextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        <Style x:Key="TextBoxStyle" TargetType="{x:Type TextBox}">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Thin"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Page.Resources>
    <Grid Background="WhiteSmoke" Grid.Row="1" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Start period" Grid.Row="0" Grid.Column="0" Style="{StaticResource TextBlockStyle}"/>
        <TextBox Text="Start period" Grid.Row="1" Grid.Column="0" Style="{StaticResource TextBoxStyle}"/>
        <Button Content="+" Grid.Row="1" Grid.Column="2" Margin="4"/>

        <TextBlock Text="End period"  Grid.Row="0" Grid.Column="1" Style="{StaticResource TextBlockStyle}"/>
        <TextBox Text="End period"  Grid.Row="1" Grid.Column="1" Style="{StaticResource TextBoxStyle}"/>
    </Grid>
</Page>
